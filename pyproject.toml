[project]
name = "online_shop_assistant_chat"
version = "1.0.0"
description = "Generated by ANQ Scaff"
authors = [
    {name = "Your Name", email = "your.email@example.com"}
]
readme = "README.md"
requires-python = ">=3.10"
keywords = ["fastapi", "api", "web"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

# 注意：依赖项在 requirements.txt 中管理
# 这是一个 Web 应用项目，不需要打包分发，因此不配置 build-system

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 120
# 不指定 target-version，让 ruff 自动检测
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]

[tool.ruff.lint]
exclude = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    ".venv",
    "venv",
]
